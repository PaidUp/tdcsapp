'use strict';

describe('Directive: footerBar', function () {

  // load the directive's module and view
  // beforeEach(module('convenienceApp'));
  // beforeEach(module('components/footerBar/footerBar.html'));

  // var element, scope;

  // beforeEach(inject(function ($rootScope) {
  //   scope = $rootScope.$new();
  // }));

  // it('should make hidden element visible', inject(function ($compile) {
  //   element = angular.element('<footer-bar></footer-bar>');
  //   element = $compile(element)(scope);
  //   scope.$apply();
  //   expect(element.text()).toBe('this is the footerBar directive');
  // }));
});
